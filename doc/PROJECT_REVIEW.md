# プロジェクトレビュー（2026-09-05）

CinemaはmacOS 14以降を対象とする、映画・映像制作のプリプロダクション用ドキュメントアプリです。SwiftUIの編集画面から作品設定・Scene State・カット・リファレンスを編集し、AIによるプレビュー生成と外部推論用Scene Bundle出力へつなぎます。現状の用途ではバックエンドサーバーの追加は不要です。

データは `StoryboardDocument` が保持し、`storyboard.json` と画像・旧形式の埋め込み動画をFileWrapperで保存します。新しい生成動画は書類の隣の `movies/` に保存し、書類には相対参照を記録します。APIキーはKeychain、画面比率は作品書類に保持し、プロバイダなどの共通設定はAppStorageにあります。

## 修正内容

- [Done] カット・ページ・ブロック削除をドキュメント層に集約。先頭カットを削除しても残るブロックの見出し・シーケンス・シーン名を引き継ぎ、他のカットやリファレンスが使用中の画像を保持。
- [Done] Scene Stateプロンプト構築をAIPromptBuilderに集約し、アプリ内の画像・動画生成にも反映。Bundleのカット別動画プロンプトにも作品全体の制作指示を追加。
- [Done] ブロック境界・日本語／英語／简体中文の場面転換判定を共通化。動画の前フレーム参照も場面転換時に解除。フレーム抽出失敗時に古いフレームを使い回さない。
- [Done] 未保存書類や内容のないカットで動画生成を開始しない。同じ書類での生成の重複開始を防止。画像生成中にカットが削除された場合、孤立画像を登録しない。
- [Done] 絵コンテのページ番号をカット番号として扱っていたリファレンス適用先を修正。
- [Done] DeepInfra動画・Novita画像／動画のダウンロードでHTTP失敗・空データを検出し、エラーページを生成メディアとして保存しない。
- [Done] Bundleの秒数で非正数・非有限値を検出し、数値メタデータへの不正値混入を防止。重複リファレンスIDでの検証クラッシュを防止。
- [Done] 設定・保存メニューと動画一覧の主要な未翻訳表示を英語・日本語・简体中文に対応。
- [Done] APIキーをmacOS Keychainへ移行。既存のUserDefaults値はKeychain保存成功後に削除。
- [Done] Result Bundle v1と取り込み画面を追加。元Bundle UUID、カットUUID、Runner・モデル版・実効パラメータ・警告を保持し、危険な相対パス・未知のカット・同一Bundleの重複取り込みを拒否。
- [Done] 画面比率とカスタム寸法を作品文書へ移行し、複数作品間で設定が混ざらないように修正。シーン動画生成のキャンセル操作と開始時設定のスナップショットも追加。

## 設計上の残課題

- [Done] シーンの識別を表示タイトルから永続UUIDに分離。既存書類を移行し、同名ブロック・改名・先頭カット削除・移動でもScene Stateとカット履歴を維持。所属不明の旧結合動画は手動関連付けに対応。
- [Done] 動画保存と復旧記録をサービスへ分離。各カットの完成時に動画と記録を保存し、途中失敗・キャンセル・書類再開時に履歴を復旧。全カット保存済みならAIを再実行せず結合可能。生成開始時設定も固定。
- [Done] 開いている書類のURL変更を監視し、参照動画と作品専用の復旧記録を新しいmoviesフォルダへコピー。元ファイルを保持し、異なる同名ファイルへの上書きを拒否。生成終了時にも開始時の保存先から追加コピーする。
- [Done] 2026-09-12：独立した「動画込みで作品コピーを保存」を追加。参照動画と復旧可能な完成済みカットを文書へ内包し、元のmoviesフォルダなしで読み込めるコピーを作成。欠損動画と保存ルート外へのシンボリックリンクはエラーにし、元の文書を変更しない。

2026-09-12追記：作品コピーと移送サービスを含め66テスト成功。標準の別名保存ダイアログで検証用書類を別フォルダへ保存し、動画ファイルのコピーと元ファイルとのバイト一致を確認。動画の内容は移送確認用データであり、映像再生の検証ではない。移動メニューと、コピー再試行・元moviesフォルダ選択の復旧操作を追加。移動ダイアログと新しい復旧メニューの実画面検証は残る。書類の移動と動画コピーは一体のトランザクションではなく、コピー失敗時は復旧操作が必要。
- [Done] 2026-09-22：生成・画像取り込みの状態とエラー、対象カットの補助ラベル、台本表示設定、ワークスペースの案内を英語・日本語・简体中文に対応。
- [Next] 小さいウインドウ・長い英語文言・複数書類、「移動」と動画保存先復旧の実画面回帰検証。
- [Later] 外部推論結果の取り込み、生成の再現性メタデータ、別プロセスでの映像処理。

## 検証範囲

2026-09-08のroadmap実装後はビルドと全58テストが成功し、git diff --checkも成功。同名シーンの移行・状態分離、改名・先頭削除・カット移動、Bundle識別子、生成途中の保存、文書未保存状態からの復旧、結合失敗・キャンセル・再結合、壊れたクリップの分離、別作品・削除済みカットの保護を検証。

変更後のアプリバンドルで検証専用の旧形式書類を開き、同名の2シーンの選択切替、Scene Stateが混ざらないこと、保存したUUIDと入力内容、主要画面の配置を画面・アクセシビリティツリーと保存ファイルで確認しました。プロジェクトはmacOS専用のため、iPhone Previewは対象外です。

有料の外部AI生成、全プロバイダの現行API互換性、実動画の結合、署名・公証・配布ビルドは今回検証していません。上記の設計課題は未解決であり、このレビューは全経路に不具合がないことを保証するものではありません。
